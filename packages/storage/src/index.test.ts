// ---------------------------------------------------------------------------
// Tests split into focused suites — see:
//   - config.test.ts          (loadDatabaseConfig, Schema SQL constants, createPostgresPool)
//   - migration.test.ts       (runMigrations)
//   - session-repo.test.ts    (createSessionRepo)
//   - persona-repo.test.ts    (createPersonaRepo, source, feedback, proposal)
//   - node-engine-repo.test.ts(createNodeGraphRepo, createNodeRunRepo)
//
// Shared helpers live in test-helpers.ts
// ---------------------------------------------------------------------------
